# AGENTS.md

## 响应与沟通

- 始终使用简体中文回答。
- 每次回复末尾必须附带单独一行 `[by=x-agent-suite]`。
- 在探索、编码、验证前，先用 1 到 2 句话说明当前目标和下一步动作。
- 完成任务时，必须说明已验证项、未验证项和剩余风险。
- 若在设计、架构、计划或实现推演中发现边界冲突、设计冲突、阶段切口冲突或隐藏的全局一致性风险，必须第一时间单独高亮提示用户，不得等到实现后或最终验收时再集中暴露。高亮提示默认使用 `**[冲突提示]**` 前缀，并明确写出：冲突点、影响范围、建议停点和是否需要先更新计划/文档。
- **任何要求在 `packages/*/src/` 内识别某个具体被测系统的需求，必须先高亮为边界冲突，再讨论实现**——不得默默满足。

## 工作方式

- 先做非破坏性探索，再进入实现。
- 变更前说明目标、范围和预期影响；如果发现方案需要扩大范围，先明确说明再继续。
- 进入会话后，先在仓库根目录执行 `rg --files --max-depth 1 -g 'AGENTS.md' -g 'AGENTS.*.md'`，仅检查根目录规则文件；不得递归搜索子目录，也不得在启动阶段枚举或扫描下级边界规则文件。
- 启动阶段的最小必要读取范围仅限仓库根目录已存在且与当前宿主、当前流程直接相关的规则文件；上一步 `rg AGENTS.*.md` 发现的根目录平台扩展即属根规则集，命中（与当前宿主/流程相关）且文件存在即读取。
- 后续如进入具体边界，再根据实际进入的目录按需感知并读取该边界内的局部规则文件。
- 优先用 `rg` 搜索文件和文本；优先使用非交互式命令。
- 不在未说明的情况下做大规模重构、跨模块接口改写或批量重命名。
- 不回退不是自己产生的改动，除非用户明确要求。
- 发现的根目录扩展按约定分层，不逐个登记：`AGENTS.local.md`（本地私有、`.gitignore` 不提交）为最高优先本地覆盖层；`AGENTS.<host>.md`（如 `AGENTS.ai.md`）为对应宿主/平台扩展，命中时在其适用范围内优先于 `AGENTS.md` 默认。冲突处优先级：本地覆盖层 > 命中的平台扩展 > `AGENTS.md`。

## 搜索与文档感知

- 遇到“项目怎么玩、模块如何组合、某工具何时使用”等玩法问题时，先读 `docs/tutorial/README.md`；需要穷举组合或核对公开工具覆盖时，再读 `docs/tutorial/catalog.json`。
- 搜索输出应包含执行的命令与命中路径/行号（`rg`/`fd` 优先）。
- 规则文件发现命令优先级：
  - 首选：`rg --files --max-depth 1 -g 'AGENTS.md' -g 'AGENTS.*.md'`
  - 备选：`fd -HI --max-depth 1 -g 'AGENTS*.md' .`
  - 兜底：`find . -maxdepth 1 \( -type f -o -type l \) -name 'AGENTS*.md'`
- 内容搜索示例：`rg -n "关键词" docs/`、`rg -n "关键词" packages/contracts/src/`。
- 文件查找示例：`rg --files packages/ | rg "pattern"`（或 `fd "pattern" packages/`）。

## 项目边界

- 本库是通用 Agent 测试套件框架：提供 driver、scenario、criterion、report 等基础设施，**不认识任何具体被测系统**。
- `packages/*/src/` 内不得出现任何具体 Agent / CLI / 宿主 / 协议 / 判据名；具体宿主 profile 与领域 scenario 由消费者注册。
- 本库不读写任何消费者仓库，不感知消费者目录结构。

## 领域中立（硬约束）

本库的全部价值建立在一条纪律上：**它不认识任何被测系统。**

- **`packages/*/src/` 内不得出现任何具体被测系统的名字、命令、环境变量、协议标记或判据名。** 这条由 `pnpm boundary` 词汇表检查强制，违反即 CI 失败。
- **零内建判据。** 本库不预置任何 metric——路由命中、工具成功、接地、产物合规这类名字全部由消费者注册。
- **不得为满足单个消费者的需求扩展契约。** 新增契约字段前先问：第二个消费者是否也需要？答案为否时，走下方合法出口，不改契约。
- **文档可以举消费者为例**（说明用途需要），**源码不可以**。这条区分是刻意的：文档用于沟通，源码用于执行。
- 本库不读写任何消费者仓库，不感知它们的目录结构，不依赖它们存在。

### 三个合法出口

领域需求必须有地方去，否则一定凿墙。凡是领域特有的东西，走这三个口，不要改契约：

| 出口                                | 用途                                                                        |
| ----------------------------------- | --------------------------------------------------------------------------- |
| `metadata: Record<string, unknown>` | Observation / DriverOpenContext / ScenarioSpec 上的自由区，kit 只透传不解释 |
| 失败类别注册                        | 领域声明自己的 reason 前缀，kit 只做分类统计，不认识语义                    |
| 插件注册表                          | driver / profile / 判据全部注册进来，kit 内不枚举取值                       |

### 边界债务留痕

确实必须穿透边界时（会有这种时候），留标记：

```ts
// BOUNDARY-DEBT(<消费者>): 为什么必须穿透、什么条件下可以回收
```

`pnpm boundary` 统计条数。**债务允许存在，但必须可枚举**——目标不是杜绝腐化，是让腐化在发生当天可见、事后可批量回收。

## 开发命令

- 安装：`pnpm install`
- 单元测试：`pnpm test`
- 集成测试：`pnpm itest`
- 类型检查：`pnpm typecheck`
- 边界守卫：`pnpm boundary`
- 收口：`pnpm check`（boundary + typecheck + test + itest；不含 token 级）

## 代码与测试

- 遵循 DRY、KISS、SOLID、YAGNI；只改需求直接涉及的代码。
- 单函数尽量不超过 50 行，单文件超过 500 行必须拆分。
- 所有导出符号写中文 JSDoc；入口和跨边界文件写模块头与不变量注释。
- 新行为先写失败测试，再实现；错误必须显式处理，禁止静默失败。

### 测试分层约定（适用全仓所有 `*test*` 文件）

- 只有两类默认测试：单元测试（`*.test.ts`，`pnpm test`）与集成测试（`*.ittest.ts`，`pnpm itest`）。
- 分类看验证对象与代价，**不看是否起子进程**：纯 fake、进程内组件、loopback 假端点、通用测试子进程均归单元测试，只要不拉起真实宿主 CLI 且零 token。
- 拉起真实宿主 CLI，并经 sandbox + 假端点验证 harness 编排或真实宿主路径的，归集成测试；该层仍必须零 token，可按显式前置条件 skip。
- live 模式打真实 provider、会产生真实 token/费用的测试统一命名 `*.token.ittest.ts`，不进入 `pnpm test`、`pnpm itest` 或 `pnpm check`。
- token 测试可与普通 itest 平铺，便于全仓打平观察；数量较多或需要权限分组时，也可按仓库需要放进 `tests/token/`。目录只负责组织，不承担安全语义；默认 runner 必须按完整后缀排除所有 `*.token.ittest.ts`。
- token 测试只能通过显式 `itest:token:*` 脚本运行，首个 token 用例落地时再添加精确入口；不得依赖目录结构来防止默认命令误收。
- 保留的终止后缀只有 `*.test.ts`、`*.ittest.ts`、`*.token.ittest.ts`；PTY、headless、sandbox 是机制，smoke/contract 是范围或目的，只写在文件 stem、测试标题和 catalog，不再组合成新的官方后缀。决策依据见 `docs/research/test-file-naming-taxonomy.md`。
- token 测试默认从仓库根 `.env.e2e.yaml`（已 gitignore）读取 live 配置；若确需借用本机登录态，测试必须自带显式 skip 闸门、最小权限 sandbox、现场清理和脱敏诊断。
- 教程的可执行验证同样遵守该分层；“PTY/headless/子进程”本身不会自动升级为 `*.ittest.ts`，只有接入真实宿主才升级。
- 完整判定表、位置与命令见 `docs/spec/testing.md`。

## 安全与文档

- 禁止硬编码密钥、token、用户目录或本机绝对路径。
- 行为、协议、使用和验证变化必须同步 `docs/`。
- 最终汇报必须列出已验证项、未验证项、剩余风险和注释收口范围。

## Git

- Commit 使用 Conventional Commits，中文摘要，不添加 AI 小尾巴。
- 提交信息不写任何消费者名，除非该次变更确实是为记录跨仓契约影响。
- 禁止 force push；未经当前会话明确授权，不执行 commit、push 或远程写入。
